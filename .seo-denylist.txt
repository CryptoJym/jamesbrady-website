# CLIENT NAME DENYLIST — one term per line, comments start with #.
#
# THIS FILE IS A PLACEHOLDER AND CONTAINS NO REAL TERMS YET.
#
# TODO(deploy-setup, blocking): before the first deploy, CI must materialize
# this file from the CLIENT_DENYLIST repository secret, created with James's
# approval. Until that secret exists, check 10 of scripts/verify-seo.mjs runs
# against an empty list and reports LOUDLY that it is proving nothing.
#
# The gate FAILS CLOSED: if this file is missing entirely, the check fails.
# It passes on an empty list ONLY while the explicit marker below is present,
# so "no denylist" can never look like "denylist clean".
#
# The gate prints file + line number + a hash of any match, NEVER the matched
# term — echoing a client name into a public CI log is the exact harm this
# gate exists to prevent.
#
# A name leaves this list only when the corresponding work entry carries a
# `clearance` record in the content source.
PLACEHOLDER-EMPTY-DENYLIST
